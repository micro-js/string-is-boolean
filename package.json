{
  "name": "@f/string-is-boolean",
  "description": "Check if string looks like a boolean.",
  "repository": "git://github.com/micro-js/string-is-boolean.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/is-boolean": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
